let valores = [3, 4, 6, 5, 9]
// valores.sort()
console.log(valores);
// para cada pos(posição) em num(vetor)
for (let pos in valores) {
  console.log(`A posição ${pos} tem o valor ${valores[pos]}`);
}